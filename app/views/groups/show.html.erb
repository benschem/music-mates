<div id="page-container-groupshow">

<div class="show">

  <div class="concert-header-center">
    <div class="img-color" style="background-image: url(<%= @concert.artist.image_url %>)"></div>
    <div class="img-gradient"></div>
    <h1 id="h1-basic-styling"> <%= @concert.artist.name %> </h1>
  </div>

  <div class="group-info-container">
    <h1>You're going!</h1>
    <div class="info-group">
      <p>📅 On <%= @concert.date.strftime("%A %e %B %Y") %></p>
      <p>From <%= @concert.date.strftime("%l:%M %P %Z") %></p>
    </div>
    <!-- Time.current.in_time_zone("America/New_York").strftime("TZ is: %Z") # => "TZ is: EDT" -->
    <div class="info-group">
      <p>📍 At <%= @concert.venue %></p>
      <p>In <%= @concert.location %></p>
    </div>
  </div>

  <h2>🧑‍🤝‍🧑 <%= pluralize(@concert.users.without_me(current_user).count, 'mate') %> are invited</h2>

    <div>
      <%= render "shared/index_avatars", concert: @concert %>
    </div>

  TODO: Button does not link to chatroom, so don't click it as it will make a new group.
  <div id="show-btn-container">
    <%= link_to "Chat with this group!", chatroom_path(@group), id:"show-btn" %>
  </div>

</div>
